-- File generated by the BNF Converter (bnfc 2.9.5).

-- | The abstract syntax of language example.

module Example.Abs where

import Prelude (Integer)
import qualified Prelude as C (Eq, Ord, Show, Read)

data Float = Float Integer
  deriving (C.Eq, C.Ord, C.Show, C.Read)

data NumType = I32 Integer | I64 Integer | F32 Float | F64 Float
  deriving (C.Eq, C.Ord, C.Show, C.Read)

data BinOp = ADD | SUM | MUL | DIV
  deriving (C.Eq, C.Ord, C.Show, C.Read)

data ArithmeticOp = ArithmeticOp NumType BinOp
  deriving (C.Eq, C.Ord, C.Show, C.Read)

